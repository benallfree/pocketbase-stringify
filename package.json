{
  "name": "pocketbase-stringify",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && tsup src/index.ts --dts --format cjs --out-dir dist",
    "watch": "bun run build --watch"
  },
  "version": "0.0.1",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
