{
  "name": "pocketbase-stringify",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "version": "0.0.1",
  "devDependencies": {
    "esbuild": "^0.23.1"
  },
  "scripts": {
    "build": "esbuild src/index.ts --format=cjs --sourcemap --bundle --outfile=dist/index.js",
    "watch": "esbuild src/index.ts --format=cjs  --watch  --sourcemap --bundle --outfile=dist/index.js"
  }
}
